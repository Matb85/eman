import { defaultlinks } from "./globals/globals";

const dirname = "/core";

export const state = () => ({
  subtreelinks: [defaultlinks(dirname)],
});

export const mutations = {};

export const actions = {};

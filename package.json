{
  "name": "redinn_core",
  "private": true,
  "peerDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "release": "lerna version --conventional-commits --no-push",
    "test": "go run ./utils/test.go",
    "test:windows": "go run ./utils/test.go && taskkill /IM \"redinn-core.exe\" /F && taskkill /IM \"node.exe\" /F"
  }
}

{
  "name": "redinn_core",
  "private": true,
  "peerDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "release": "lerna version --conventional-commits --no-push",
    "test": "go run ./test.go"
  }
}
